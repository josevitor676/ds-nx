import { Meta } from "@storybook/addon-docs/blocks";
import { defaultTheme } from "../theme";
import { CopyToken } from "./components/CopyToken";
import { DocAccordion } from "./components/DocAccordion";

<Meta title="Tokens/Borders & Shadows" />

# Borders & Shadows

<div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginTop: '24px' }}>

<DocAccordion
  label="Border Radius"
  subtitle="Escalas de arredondamento"
  defaultOpen={true}
>
  <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
    {Object.entries(defaultTheme.tokens.borderRadius).map(([key, value]) => (
      <div
        key={key}
        style={{
          display: "flex",
          alignItems: "center",
          gap: "20px",
          padding: "4px 0",
        }}
      >
        <div
          style={{
            width: "56px",
            height: "56px",
            flexShrink: 0,
            border: "2px solid #0B109F",
            borderRadius: value,
            backgroundColor: "rgba(11,16,159,0.05)",
          }}
        />
        <div
          style={{
            display: "flex",
            gap: "8px",
            alignItems: "center",
            flexWrap: "wrap",
          }}
        >
          <CopyToken text={`--ds-radius-${key}`}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#363843",
              }}
            >
              --ds-radius-{key}
            </span>
          </CopyToken>
          <CopyToken text={value}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#7C8096",
              }}
            >
              {value}
            </span>
          </CopyToken>
        </div>
      </div>
    ))}
  </div>
</DocAccordion>

<DocAccordion label="Shadows" subtitle="Escalas de sombra" defaultOpen={true}>
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    {Object.entries(defaultTheme.tokens.shadows).map(([key, value]) => (
      <div
        key={key}
        style={{
          display: "flex",
          alignItems: "center",
          gap: "20px",
          padding: "4px 0",
        }}
      >
        <div
          style={{
            width: "80px",
            height: "56px",
            flexShrink: 0,
            borderRadius: "8px",
            boxShadow: value,
            backgroundColor: "#ffffff",
            border: "1px solid rgba(0,0,0,0.04)",
          }}
        />
        <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>
          <CopyToken text={`--ds-shadow-${key}`}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#363843",
              }}
            >
              --ds-shadow-{key}
            </span>
          </CopyToken>
          <CopyToken text={value}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#7C8096",
              }}
            >
              {value}
            </span>
          </CopyToken>
        </div>
      </div>
    ))}
  </div>
</DocAccordion>

</div>
