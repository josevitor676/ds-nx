import { Meta } from "@storybook/addon-docs/blocks";
import { themeTypography } from "../theme/tokens-adapter";
import { CopyToken } from "./components/CopyToken";
import { DocAccordion } from "./components/DocAccordion";

<Meta title="Tokens/Typography" />

# Typography

<div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginTop: '24px' }}>

<DocAccordion label="Font Family" defaultOpen={true}>
  <div
    style={{
      display: "flex",
      alignItems: "center",
      gap: "24px",
      padding: "8px 0",
    }}
  >
    <span
      style={{
        fontSize: "32px",
        fontFamily: themeTypography.fonts.body,
        color: "#202125",
      }}
    >
      Aa
    </span>
    <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>
      <CopyToken text="--ds-font-family">
        <span
          style={{
            fontFamily: "monospace",
            fontSize: "13px",
            color: "#363843",
          }}
        >
          --ds-font-family
        </span>
      </CopyToken>
      <CopyToken text={themeTypography.fonts.body}>
        <span
          style={{
            fontFamily: "monospace",
            fontSize: "13px",
            color: "#7C8096",
          }}
        >
          {themeTypography.fonts.body}
        </span>
      </CopyToken>
    </div>
  </div>
</DocAccordion>

<DocAccordion label="Font Sizes" defaultOpen={true}>
  <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>
    {Object.entries(themeTypography.fontSizes)
      .sort(([a], [b]) => Number(a) - Number(b))
      .map(([key, value]) => (
        <div
          key={key}
          style={{
            display: "flex",
            alignItems: "center",
            gap: "12px",
            padding: "4px 0",
          }}
        >
          <CopyToken text={`--ds-font-size-${key}`}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#363843",
                width: "165px",
                display: "inline-block",
              }}
            >
              --ds-font-size-{key}
            </span>
          </CopyToken>
          <CopyToken text={value}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#7C8096",
                width: "40px",
                display: "inline-block",
              }}
            >
              {value}
            </span>
          </CopyToken>
          <span
            style={{
              fontSize: value,
              lineHeight: 1,
              fontFamily: themeTypography.fonts.body,
              color: "#202125",
            }}
          >
            Aa
          </span>
        </div>
      ))}
  </div>
</DocAccordion>

<DocAccordion label="Font Weights" defaultOpen={true}>
  <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
    {Object.entries(themeTypography.fontWeights).map(([key, value]) => (
      <div
        key={key}
        style={{
          display: "flex",
          alignItems: "center",
          gap: "12px",
          padding: "4px 0",
        }}
      >
        <CopyToken text={`--ds-font-weight-${key}`}>
          <span
            style={{
              fontFamily: "monospace",
              fontSize: "13px",
              color: "#363843",
              width: "195px",
              display: "inline-block",
            }}
          >
            --ds-font-weight-{key}
          </span>
        </CopyToken>
        <CopyToken text={value}>
          <span
            style={{
              fontFamily: "monospace",
              fontSize: "13px",
              color: "#7C8096",
              width: "80px",
              display: "inline-block",
            }}
          >
            {value}
          </span>
        </CopyToken>
        <span
          style={{
            fontSize: "16px",
            fontWeight: value,
            fontFamily: themeTypography.fonts.body,
            color: "#202125",
          }}
        >
          A raposa rápida salta sobre o cão preguiçoso
        </span>
      </div>
    ))}
  </div>
</DocAccordion>

<DocAccordion label="Line Heights" defaultOpen={true}>
  <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>
    {Object.entries(themeTypography.lineHeights)
      .sort(([a], [b]) => Number(a) - Number(b))
      .map(([key, value]) => (
        <div
          key={key}
          style={{
            display: "flex",
            alignItems: "center",
            gap: "12px",
            padding: "4px 0",
          }}
        >
          <CopyToken text={`--ds-line-height-${key}`}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#363843",
                width: "185px",
                display: "inline-block",
              }}
            >
              --ds-line-height-{key}
            </span>
          </CopyToken>
          <CopyToken text={value}>
            <span
              style={{
                fontFamily: "monospace",
                fontSize: "13px",
                color: "#7C8096",
                width: "44px",
                display: "inline-block",
              }}
            >
              {value}
            </span>
          </CopyToken>
          <div style={{ display: "flex", alignItems: "center", gap: "4px" }}>
            <div
              style={{
                width: "3px",
                height: value,
                backgroundColor: "#0B109F",
                borderRadius: "2px",
                flexShrink: 0,
              }}
            />
          </div>
        </div>
      ))}
  </div>
</DocAccordion>

</div>
